{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db": "node server.js",
    "backend": "cd ../backend && npm run moleculer --silent",
    "dev": "concurrently \"npm run db\" \"npm run backend\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "moleculer": "^0.14.35"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
